{
  "name": "coupleink",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-index.js && vite build && node scripts/publish-static.js",
    "preview": "vite preview",
    "start": "node app.js"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "leaflet": "^1.9.4"
  }
}
